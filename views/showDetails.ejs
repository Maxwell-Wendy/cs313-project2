<!DOCTYPE html>
<html lang="en">
<head>
        <title>Book Catalog</title>   
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"></script>
        
</head>
<body class="container">
    <header>
        <div class="jumbotron text-center" style="margin-bottom:0">
            <h1>Search Google Books</h1>        
        </div>
                    
        <div class="container" style="margin-top:30px">
            <p>Enter a book title to search:</p>
            <form class="form-group" action="/searchAPI" method="POST">
                <input type="text" class="form-control" name="searchText">
                <input type="submit" class="btn btn-outline-primary" value="Search">
            </form>
        </div>
    </header>

    <h3>Here are the details for your selected book:</h3>
    
    <img src="<%= imageURL %>" class="img-thumbnail" alt="Book Image">
    <p>Title: <strong><%= title%></strong></p>
    <p>Author: <%= author %></p>
    <p>Publisher: <% publisher %></p>
    <p>Date published: <%= publishedDate %></p>
    <p>Description: <%= description %></p>
    <p>Id: <%= id %></p>

    <p>Would you like to add this book to your personal library?</p>
    <form class="form-group" action="/saveBook" method="POST">
        <input type="hidden" name="googleBookID" value="<%= id %>">
        <input type="hidden" name="title" value="<%= title %>">
        <input type="hidden" name="author" value="<%= author %>">

        <input type="submit" class="btn btn-outline-primary" value="Add to my Catalog">
    </form>
</body>
</html>
